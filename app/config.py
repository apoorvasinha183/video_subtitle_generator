# app/config.py

# Duration of each audio chunk in milliseconds
CHUNK_LENGTH_MS = 1000  # 1 seconds

# Overlap between chunks in milliseconds (to smooth transitions)
CHUNK_OVERLAP_MS = 100  # 1 second

# Temporary folder for intermediate audio files
TEMP_AUDIO_DIR = "temp_audio"
